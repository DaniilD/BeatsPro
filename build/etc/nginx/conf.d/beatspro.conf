server  {
    listen 80 default_server;
    server_name api.beatspro.space;

    #proxy_pass _:8090;

  #  root /srv

    location / {
       # fastcgi_pass unix:/tmp/go.sock;
        proxy_pass http://api-beatspro:8090;
      #  include fastcgi_params;
    }
}